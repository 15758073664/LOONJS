// ⚠️ 只能用于 Rewrite
const headers = $request.headers;
const cookie = headers["Cookie"] || headers["cookie"];

if (cookie) {
  $persistentStore.write(cookie, "UNICOM_COOKIE");
  $notification.post("中国联通", "Cookie 获取成功", "");
} else {
  $notification.post("中国联通", "未获取到 Cookie", "");
}

$done({});
